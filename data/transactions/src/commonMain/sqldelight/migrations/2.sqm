-- Add payment modes table
CREATE TABLE IF NOT EXISTS PaymentMode (
    id         INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    name       TEXT    NOT NULL,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);

-- Add payment_mode_id to TransactionEntry (nullable)
ALTER TABLE TransactionEntry ADD COLUMN payment_mode_id INTEGER;
